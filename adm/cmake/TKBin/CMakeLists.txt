SET(TOOLKIT TKBin)

SET(TOOLKIT_MODULES
	BinDrivers
	BinMDataXtd
	BinMNaming
	BinTools
)
SET(TOOLKIT_DEPENDS
	TKMath
	TKernel
	TKBinL
	TKLCAF
	TKG2d
	TKCAF
	TKBRep
	TKG3d
	TKCDF
)


IF (${PROJECT_NAME}_VISUALISATION)

	SET(TOOLKIT_MODULES ${TOOLKIT_MODULES}
		BinMPrsStd
		)

	SET(TOOLKIT_DEPENDS ${TOOLKIT_DEPENDS}
		TKV3d
		)
ENDIF()


INCLUDE(../BuildToolkit.cmake)

